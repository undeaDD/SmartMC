{
  "name": "@smart-mc/protocol",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "generated/ts/index.ts",
  "types": "generated/ts/index.ts",
  "scripts": {
    "generate": "bun run generate:jsonschema && bun run generate:ts",
    "generate:jsonschema": "tsp compile schema",
    "generate:ts": "bun run scripts/generate-ts.ts",
    "typecheck": "tsc --noEmit -p ."
  },
  "devDependencies": {
    "@types/json-schema": "^7.0.15",
    "@typespec/compiler": "^1.0.0",
    "@typespec/json-schema": "^1.0.0",
    "bun-types": "^1.3.0",
    "json-schema-to-typescript": "^15.0.4",
    "typescript": "^5.7.3"
  }
}
